# map-full-workflow-not-toy-slice
Model the whole real workflow end-to-end, not a 20% slice. A passing test on a toy subset still leaves the user unable to do the job. Research the real lifecycle; don't infer it from a half-built UI.
