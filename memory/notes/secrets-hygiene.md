# secrets-hygiene
Never commit/print/hardcode real credentials, keys, emails, project refs, or tokens. .gitignore excludes all .env* except .env.example (placeholders only). If a secret is exposed, rotate it.
