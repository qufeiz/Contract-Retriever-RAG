# AI-native doc hygiene
Keep active docs true to the code in the SAME change that alters the code. CLAUDE.md is a map + landmines (a per-session token tax), not a manual — depth lives in docs/, read just-in-time. doc-lint catches broken links/dead refs; a stale *description* is human judgment.
