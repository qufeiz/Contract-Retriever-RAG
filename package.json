{
  "name": "contract-retriever-rag",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI Business Knowledge Assistant — NL question → query routing → hybrid SQL+RAG retrieval → grounded, cited answer.",
  "//": "Every script here is a forcing function. A forcing function is INERT until it is a REQUIRED CI step (.github/workflows/ci.yml). doc-lint + doc-structure-lint are wired real; lint/typecheck/test/test:journeys gain teeth the moment they point at real toolchain.",
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:index && next build",
    "build:index": "node --experimental-strip-types scripts/build-index.mts",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/unit/*.test.mts",
    "test:journeys": "playwright test --config=playwright.journeys.config.ts",
    "doc-lint": "node scripts/doc-lint.mjs",
    "doc-structure-lint": "node scripts/doc-structure-lint.mjs",
    "verify:release": "npm run doc-lint && npm run doc-structure-lint && npm run typecheck && npm run test"
  },
  "dependencies": {
    "next": "15.5.19",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "@xenova/transformers": "2.17.2",
    "better-sqlite3": "11.8.1"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "@types/better-sqlite3": "7.6.12",
    "@types/node": "22.10.7",
    "@types/react": "19.0.7",
    "@types/react-dom": "19.0.3",
    "typescript": "5.7.3"
  },
  "engines": {
    "node": ">=22.6"
  }
}
