# Copy to .env.local (gitignored) and fill in real values. NEVER commit real values.
# DeepSeek (OpenAI-compatible) — LLM for query routing + grounded generation.
LLM_API_KEY=your-deepseek-api-key-here
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
# The mock data is anchored to this date (e.g. "38 contracts expire within 90 days").
# Pin it so date-relative answers are deterministic regardless of when the demo is viewed.
# Omit to use the real current date.
ASSISTANT_TODAY=2026-06-09
